{{- if and .Values.elastic.enabled .Values.elastic.metricbeat.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-elastic-metricbeat" .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elastic-metricbeat
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/elastic-metricbeat-configmap.yaml") . | sha256sum }}
      labels:
        app: elastic-metricbeat
    spec:
      restartPolicy: {{ .Values.elastic.metricbeat.restartPolicy  | quote }}
      containers:
        - name: {{ printf "%s-guacamole" .Release.Name }}
          image: docker.elastic.co/beats/metricbeat:{{ .Values.guacamole.version }}
          imagePullPolicy: {{ .Values.elastic.metricbeat.imagePullPolicy  | quote }}
          env:
          {{- range $secretname, $secret := .Values.elastic.metricbeat.secrets }}
            - name: {{ $secretname | upper }}
              valueFrom:
                secretKeyRef:
                  name: {{ printf "$s-elastic-metricbeat" $.Release.Name }}
                  key: {{ $secretname }}
          {{- end }}
          volumeMounts:
            - name: {{ printf "%s-elastic-metricbeat-volume" .Release.Name }}
              mountPath: "/usr/share/metricbeat/metricbeat.yml"
              subPath: "metricbeat.yml"
      volumes:
        - name: {{ printf "%s-elastic-metricbeat-volume" .Release.Name }}
          configMap:
            name: {{ printf "%s-elastic-metricbeat" .Release.Name }}
            items:
            - key: metricbeat.yml
              path: metricbeat.yml
{{- end }}
