{{- if and .Values.elastic.enabled .Values.elastic.filebeat.enabled (gt (len .Values.elastic.filebeat.tcpPorts) 0)}}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-elastic-filebeat-tcp" .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
  {{- if .Values.elastic.filebeat.sharedIp }}
  annotations:
    metallb.universe.tf/allow-shared-ip: {{ tpl .Values.elastic.filebeat.loadBalancerIpAddressTCP . }}
  {{- end }}
spec:
  type: LoadBalancer
  loadBalancerIP: {{ tpl .Values.elastic.filebeat.loadBalancerIpAddressTCP . }}
  {{- if not .Values.elastic.filebeat.sharedIp }}
  externalTrafficPolicy: Local
  {{- end }}
  ports:
    {{- range $port := .Values.elastic.filebeat.tcpPorts }}
    - protocol: TCP
      port: {{ $port }}
      targetPort: {{ $port }}
    {{- end }}
  selector:
    app: elastic-filebeat
{{- end }}
