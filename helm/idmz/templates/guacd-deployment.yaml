{{- if .Values.guacamole.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-guacd" .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: {{ .Values.guacamole.guacd.replicas }}
  selector:
    matchLabels:
      app: guacd
  template:
    metadata:
      labels:
        app: guacd
    spec:
      restartPolicy: {{ .Values.guacamole.guacd.restartPolicy | quote}}
      containers:
        - name: {{ printf "%s-guacd" .Release.Name }}
          image: guacamole/guacd:{{ .Values.guacamole.guacd.version }}
          imagePullPolicy: {{ .Values.guacamole.guacd.imagePullPolicy | quote}}
          ports:
            - containerPort: 4822
          volumeMounts:
            - name: {{ printf "%s-guacd-shared-volume" .Release.Name }}
              mountPath: "/shared/"
          startupProbe:
            tcpSocket:
              port: 4822
            failureThreshold: 45
            periodSeconds: 1
          readinessProbe:
            tcpSocket:
              port: 4822
            initialDelaySeconds: 1
            periodSeconds: 1
          livenessProbe:
            tcpSocket:
              port: 4822
            initialDelaySeconds: 1
            periodSeconds: 1
      volumes:
        - name: {{ printf "%s-guacd-shared-volume" .Release.Name }}
          emptyDir: {}
{{- end }}