{{- if and .Values.elastic.enabled .Values.elastic.filebeat.enabled (gt (len .Values.elastic.filebeat.udpPorts) 0) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-elastic-filebeat-udp" .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
  annotations:
    metallb.universe.tf/allow-shared-ip: {{ tpl .Values.elastic.filebeat.loadBalancerIpAddress . }}
spec:
  type: LoadBalancer
  loadBalancerIP: {{ tpl .Values.elastic.filebeat.loadBalancerIpAddress . }}
  ports:
    {{- range $port := .Values.elastic.filebeat.udpPorts }}
    - protocol: UDP
      port: {{ $port }}
      targetPort: {{ $port }}
    {{- end }}
  selector:
    app: elastic-filebeat
{{- end }}
